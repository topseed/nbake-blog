a.ui.blurring.fluid.blue.card(href=prefix+id)
	.image
		img(src=prefix+id+'/'+image)
	.content
		.header #{title}
		.meta #{created}
		.description
			| #{content_html}
	.extra.content
		| Author info here
