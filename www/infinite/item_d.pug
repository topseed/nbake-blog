a.ui.blurring.fluid.blue.card(href=prefix+url+'/')
	.image
		img(src=prefix+url+'/'+image)
	.content
		.header #{title}
		.meta #{created}
		.description
			| #{content_html}
	.extra.content
		| Author info here
